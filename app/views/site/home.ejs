<!DOCTYPE html>
	<html>
	   <head>
		   <meta charset="utf-8">
		   <title>Inrodução ao Node JS </title>
	   </head>
	   <body>
		   <h1>Pagina Home</h1>
		   <% var texto= "Trabalhando com EJS"; %>
		   <p><%= texto %></p>
			<ul>
			   <% for(var chaves in clientes){%>
				   <li><%= clientes[chaves].nome%> - <%= clientes[chaves].email%></li>
			   <%}%>
	   	    </ul>
			<br>
			<%if(erros){%>
			<ul>
			   <% for(var chaves in erros){%>
				   <li><%= erros[chaves].msg%></li>
			   <%}%>
	   	    </ul>
			<%}%>
			<form action="/" method="post">
				<label>Nome:</label>
				<input type="text" name="nome" value="<%= dados.nome %>" placeholder="Maria"></input>
				<br><br>
				<label>E-mail:</label>
				<input type="text" name="email" value="<%= dados.email %>" placeholder="maria@mail.com"></input>
				<button type="submit">Adicionar</button>
			</form>
			<br>

	   </body>
	</html>
